RAG_ANSWER_PROMPT = """你是企业知识库助手。请仅根据下面「参考资料」回答用户问题。
要求：
1) 回答要简洁、分点列出（如适用）
2) 若资料不足以回答，请明确说明「知识库中未找到足够依据」，不要编造事实
3) 可在结尾用「引用」列出你主要依据的片段 id

对话历史（可能为空）：
{history}

参考资料：
{context}

用户问题：{question}
"""


RAG_RERANK_PROMPT = """你是检索重排序器（reranker）。你会看到若干候选知识片段，请挑选最能回答问题的片段 id。

规则：
1) 只输出一个 JSON 数组，元素为整数 id（例如：[12, 3, 9]）
2) 最多输出 {keep} 个 id
3) 如果资料不足以回答，也仍然输出你认为“最相关”的 1-3 个 id（不要输出空数组）
4) 不要输出任何多余文字

用户问题：
{question}

候选片段：
{candidates}
"""


RAG_QUERY_REWRITE_PROMPT = """你是检索查询改写器。根据用户问题与对话历史，生成一条更适合「向量检索 + 关键词检索」的中文 query。
要求：
1) 只输出一行，不要引号、不要解释
2) 保留关键实体（编号、制度名、部门名等）
3) 若用户问题含指代，结合历史补全为可独立检索的句子

对话历史（可能为空）：
{history}

用户问题：
{question}
"""


RAG_MULTI_QUERY_PROMPT = """你是检索查询扩展器。根据用户问题与对话历史，生成 {n} 条不同角度的中文检索 query（同义改写、同义词扩展、拆分子问题）。
要求：
1) 只输出一个 JSON 数组，元素为字符串，长度恰好 {n}
2) 不要输出任何多余文字

对话历史（可能为空）：
{history}

用户问题：
{question}
"""
