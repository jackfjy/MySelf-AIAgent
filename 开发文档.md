# 开发文档

面向在本仓库内二次开发、扩展节点与部署改造的开发者。终端用户请阅读根目录 **[用户手册.md](./用户手册.md)**。

---

## 1. 工程定位

基于 **Python + LangChain + LangGraph** 的示例工程，包含两条可独立演进的主线：

| 主线 | 说明 |
|------|------|
| 内容生成 | `Research → Writer → Editor` 线性图，状态为 `AgentState` |
| RAG 问答 | `rewrite_query → retrieve → rerank → generate_answer`，状态为 `RagState`，带 `MemorySaver` 与 `thread_id` |

---

## 2. 目录结构

```text
pro/
├── README.md                 # 索引（指向本文件与用户手册）
├── 开发文档.md               # 本文件
├── 用户手册.md               # 安装、配置、命令与测试
├── requirements.txt
├── .env.example
├── data/docs/                # 知识库源文档（入库输入）
├── data/kb/                  # 向量索引落盘（入库输出，可 .gitignore）
└── src/
    ├── api/                  # FastAPI（HTTP）
    ├── services/             # 业务封装（CLI 与 API 共用）
    ├── main.py               # 内容生成 CLI
    ├── config.py             # Settings；从项目根 .env 加载
    ├── llm_client.py         # LLMClient（ChatOpenAI + 代理/Base）
    ├── http_utils.py         # normalize_http_proxy_url
    ├── prompts.py            # 内容生成提示词
    ├── agent_nodes.py        # research / writer / editor 纯函数
    ├── state.py              # AgentState（TypedDict）
    ├── graph_builder.py      # build_content_graph
    └── rag/
        ├── main.py           # RAG 问答 CLI
        ├── ingest.py         # 入库 CLI
        ├── state.py          # RagState
        ├── prompts.py        # 回答 / rerank / 改写 / 多查询提示词
        ├── embeddings.py     # build_openai_embeddings
        ├── vector_store.py   # SimpleVectorStore（向量 + BM25）
        ├── nodes.py          # 各 LangGraph 节点实现
        └── graph_builder.py  # build_rag_graph（含 checkpointer）
```

---

## 3. 配置加载约定

- **`src/config.py`**：`load_dotenv(_PROJECT_ROOT / ".env")`，**始终从项目根**加载，避免工作目录变化导致读不到 Key。
- **RAG Embedding**：`src/rag/embeddings.py` 可与 Chat 共用 `OPENAI_*`；也可用 `EMBEDDING_OPENAI_API_KEY` / `EMBEDDING_OPENAI_BASE_URL` 单独指定。

---

## 4. 核心类与模块（按职责）

### 4.1 `LLMClient`（`src/llm_client.py`）

- 封装 `ChatOpenAI`，对外统一 `invoke(LLMInput) -> LLMOutput`。
- `PROVIDER=openai`：`OPENAI_BASE_URL` 为 **API 根**；`OPENAI_HTTP_PROXY` 为 **本机 HTTP 隧道代理**，二者勿混用。
- `PROVIDER=deepseek`：`DEEPSEEK_BASE_URL` + `DEEPSEEK_API_KEY`。

### 4.2 内容生成图（`src/graph_builder.py`）

- `StateGraph(AgentState)`：`START → research → writer → editor → END`。
- 节点闭包捕获 `llm`，节点返回 partial state 合并进全局 state。

### 4.3 RAG：`SimpleVectorStore`（`src/rag/vector_store.py`）

- 持久化：`chunks.jsonl` + `embeddings.npy`。
- `similarity_search`：余弦相似度；可选 `source_contains` 对 `metadata["source"]` 子串过滤。
- `bm25_search`：`rank_bm25.BM25Okapi`；分词为极简版（中文按字、英文按词），生产环境可替换为专业分词。

### 4.4 RAG 节点（`src/rag/nodes.py`）

| 节点 | 职责 |
|------|------|
| `rewrite_query_node` | 根据 `--rewrite` / `--multi-query` 生成 `retrieval_queries` |
| `retrieve_node` | 对每个 query 做 vec+BM25，多路合并按 chunk id 取最高分 |
| `rerank_node` | 可选 LLM 从候选中选 id，写入 `reranked_context` |
| `answer_node` | 用 `reranked_context` 或回退 `retrieved_context` + `history` 生成答案并追加 history |

**注意**：LangGraph 中 **节点名不得与 `RagState` 的 key 同名**（例如曾用 `answer` 作节点名会与 `answer` 字段冲突），当前生成节点名为 `generate_answer`。

### 4.5 RAG 图与记忆（`src/rag/graph_builder.py`）

- 流程：`START → rewrite_query → retrieve → rerank → generate_answer → END`。
- `MemorySaver()`：`invoke(..., config={"configurable": {"thread_id": ...}})` 持久化会话级 state（含 `history`）。

### 4.6 入口与 `sys.path`（`src/main.py`、`src/rag/main.py`）

- 支持 `python -m src.main` / `python -m src.rag.main`。
- `src/main.py` 含将项目根加入 `sys.path` 的逻辑，便于 IDE 直接运行脚本文件。

### 4.7 HTTP 层（FastAPI，`src/api/`）

- **`src/api/main.py`**：`FastAPI` 应用；`lifespan` 中初始化 `llm`、`content_graph`、`embeddings`、`vector_store`；CORS 由环境变量 `CORS_ORIGINS`（逗号分隔）控制。
- **`src/api/schemas.py`**：Pydantic 请求/响应模型。
- **`src/api/routers/content.py`**：`POST /api/v1/content/generate` → `run_content_generation`。
- **`src/api/routers/rag.py`**：`POST /api/v1/rag/query` → `run_rag_query`。
- **`src/services/`**：`content_service` / `rag_service` 封装 `graph.invoke`，CLI 与 HTTP **共用**。

启动（项目根）：`uvicorn src.api.main:app --reload --host 0.0.0.0 --port 8000`。

---

## 5. 扩展建议（开发向）

- **向量库**：将 `SimpleVectorStore` 替换为 pgvector / Milvus 等，保持 `retrieve_node` 输入输出契约不变即可渐进迁移。
- **权限**：将 `source_contains` 扩展为结构化 filter（部门、项目、ACL），在 `similarity_search` / `bm25_search` 前统一裁剪候选 id。
- **Rerank**：当前为 LLM rerank；可换 cross-encoder 或托管 rerank API，节点内替换实现即可。
- **观测**：在节点前后打结构化日志（thread_id、query、top chunks、latency、token），便于企业落地。

---

## 6. 依赖与版本说明

- 见 `requirements.txt`。若 `langgraph==0.3.0` 在 PyPI 上不可用或被标记为 yanked，可改为同 **0.3.x** 线未撤包版本，一般无需改业务代码。
- RAG 额外依赖：`numpy`、`rank-bm25`。
- HTTP：`fastapi`、`uvicorn`。

---

## 7. 许可证与免责

示例代码仅供学习与小范围试验；生产环境需自行补充安全、审计、合规与 SLA 相关能力。
