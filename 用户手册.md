# 用户手册

面向使用本工程完成**安装、配置、运行与验证**的用户。架构与类说明见 **[开发文档.md](./开发文档.md)**。

---

## 1. 本工程能做什么

- **自动写文章流水线**：按主题做研究摘要 → 写初稿 → 润色（`python -m src.main`）。
- **企业知识库问答（RAG）**：把 Markdown/文本入库，再对知识库提问并得到带引用的回答（`python -m src.rag.ingest` + `python -m src.rag.main`）。

---

## 2. 环境要求

- **Python**：建议 3.10 及以上。
- **网络**：调用大模型 API；若需翻墙，请配置本机代理（见下文 `OPENAI_HTTP_PROXY`）。

---

## 3. 安装步骤（新电脑部署）

### 3.1 获取代码

- 使用 **Git**：`git clone <仓库地址>` 后 `cd` 到项目根目录。  
- 或使用 U 盘/压缩包拷贝整个项目文件夹，保证 **`src/`、`requirements.txt`、`.env.example` 等在**同一层**（与下文「项目根目录」一致）。

### 3.2 Python 与依赖

- 建议 **Python 3.10+**，可使用 **venv**（推荐，避免污染全局环境）：

  ```bash
  python -m venv .venv
  .venv\Scripts\activate
  ```

  Linux / macOS：`source .venv/bin/activate`

- 在**项目根目录**安装依赖：

  ```bash
  pip install -r requirements.txt
  ```

### 3.3 环境变量文件

复制模板并编辑（Windows）：

```bash
copy .env.example .env
```

Linux / macOS：

```bash
cp .env.example .env
```

根据服务商填写 Key、模型名等（详见下表）。**不要将含密钥的 `.env` 提交到 Git。**

### 3.4 部署检查清单（可逐项打勾）

| 步骤 | 说明 |
|------|------|
| 项目根目录正确 | 运行 `uvicorn` 时当前目录包含 `src` 文件夹 |
| 依赖已装 | `pip install -r requirements.txt` 无报错 |
| `.env` 已配置 | 至少 `OPENAI_API_KEY`、模型名等与服务商一致 |
| 后端可启动 | 见 §5.4，能打开 `/docs` |
| 可选前端 | 见 `frontend/README.md`，Node 与 `npm install` |

---

## 4. 环境变量说明（`.env`）

| 变量 | 说明 |
|------|------|
| `PROVIDER` | `openai` 或 `deepseek` |
| `OPENAI_API_KEY` | OpenAI 或兼容接口的密钥 |
| `OPENAI_BASE_URL` | 第三方/自建 **OpenAI 兼容 API 的根地址**（通常含 `/v1`）。**不要**填进 `OPENAI_HTTP_PROXY` |
| `OPENAI_HTTP_PROXY` | 本机 **HTTP 代理**（如 Clash：`http://127.0.0.1:7890`）。不需要可留空 |
| `MODEL_NAME` | 对话模型名称 |
| `DEEPSEEK_API_KEY` / `DEEPSEEK_BASE_URL` | 仅当 `PROVIDER=deepseek` 时使用 |
| `KB_DATA_DIR` | RAG 索引存放目录，默认 `data/kb` |
| `OPENAI_EMBEDDING_MODEL` | 向量模型，默认 `text-embedding-3-small` |
| `EMBEDDING_OPENAI_API_KEY` / `EMBEDDING_OPENAI_BASE_URL` | 可选；不填则与对话共用 `OPENAI_*` |

说明：程序从**项目根目录**的 `.env` 读取配置，请保证 `.env` 与 `src` 同级。

---

## 5. 使用方法

### 5.1 内容生成（研究 → 写作 → 编辑）

```bash
python -m src.main --topic "你的主题"
```

也可指定主题：

```bash
python -m src.main --topic "AI 在教育中的应用"
```

### 5.2 知识库：入库

将文档放入 `data/docs/`（支持子目录），然后在项目根执行：

**全量重建索引（会清空旧索引）：**

```bash
python -m src.rag.ingest --path data/docs --clear
```

**仅追加新文件（不清空）：** 去掉 `--clear`。

### 5.3 知识库：问答

```bash
python -m src.rag.main -q "你的问题"
```

### 5.4 HTTP API（FastAPI）

安装依赖已包含 `fastapi`、`uvicorn`。在**项目根目录**启动服务：

```bash
uvicorn src.api.main:app --reload --host 0.0.0.0 --port 8000
```

部署到其他机器时，将下面地址中的 `127.0.0.1` 换成服务器 IP 或域名，并放行防火墙端口（默认 **8000**）。

#### 接口文档（优先看这个）

| 地址 | 说明 |
|------|------|
| **http://127.0.0.1:8000/docs** | **Swagger UI**：列出全部路径、请求体、可在线「Try it out」试调 |
| **http://127.0.0.1:8000/redoc** | ReDoc：只读、适合打印或给非开发人员阅读 |
| **http://127.0.0.1:8000/openapi.json** | OpenAPI 3.x 原始 JSON，可导入 Postman、Apifox、Insomnia 等 |

字段、枚举、默认值以 **`/docs` 页面为准**（随代码变更自动更新）。

#### 业务接口一览

| 路径 | 方法 | 说明 |
|------|------|------|
| `/` | GET | 返回简短 JSON，内含文档与健康检查等链接 |
| `/health` | GET | 健康检查 |
| `/api/v1/content/generate` | POST | 内容生成（JSON body：`topic` 等，见 Swagger） |
| `/api/v1/rag/query` | POST | 知识库问答（JSON：`question`、`thread_id`、检索选项等） |
| `/api/v1/rag/ingest` | POST | **multipart 表单**：文件字段 `files`（可多文件），表单字段 `clear`、`max_chars`；支持 md/txt/pdf/doc/docx 等，详见 Swagger |
| `/api/v1/rag/uploads` | GET | 列出已在服务器落盘的上传记录（用于「再次入库」） |
| `/api/v1/rag/reingest` | POST | **JSON body**：`ids`（上传记录 id 列表）、`clear`、`max_chars` |

**说明**：`ingest` 为 `multipart/form-data`，不要用 curl 手写错误的 `boundary`；复杂场景请用 Swagger 生成请求，或使用 Postman。

**内容生成示例（curl，Windows `^` 换行）：**

```bash
curl -X POST "http://127.0.0.1:8000/api/v1/content/generate" ^
  -H "Content-Type: application/json" ^
  -d "{\"topic\": \"人工智能与教育\"}"
```

**RAG 问答示例：**

```bash
curl -X POST "http://127.0.0.1:8000/api/v1/rag/query" ^
  -H "Content-Type: application/json" ^
  -d "{\"question\": \"年假至少多少天？\", \"thread_id\": \"web1\"}"
```

**历史上传列表：**

```bash
curl "http://127.0.0.1:8000/api/v1/rag/uploads"
```

跨域：可通过环境变量 **`CORS_ORIGINS`** 配置，多个 origin 用英文逗号分隔；默认 `*`（仅建议开发环境使用）。生产环境应改为具体前端域名。

### 5.5 Web 前端（可选）

仓库内 **`frontend/`** 为 Vue 3 + Vite 页面（上传、对话、历史再次入库）。安装 Node 后：

```bash
cd frontend
npm install
npm run dev
```

详见 **`frontend/README.md`**。生产构建与 Nginx 反代说明也在该文件中。

---

## 6. 问答命令参数（常用）

| 参数 | 含义 |
|------|------|
| `-q` / `--question` | 用户问题（必填） |
| `--top-k` | 每个检索 query 下，向量与 BM25 各取的条数，合并后再去重 |
| `--thread-id` | 会话 ID；相同 ID 共享多轮对话记忆 |
| `--source-contains` | 只检索「来源路径」中包含该子串的片段（如 `hr/`、`sample.md`） |
| `--rewrite` | 查询改写（与 `--multi-query` 二选一） |
| `--multi-query` | 多查询扩展（与 `--rewrite` 二选一） |
| `--multi-query-n` | 多查询条数，约 2～6，默认 3 |
| `--rerank` | 对候选片段做 LLM 重排序（多一次模型调用） |
| `--rerank-keep` | rerank 后保留的片段数 |

运行成功后，终端可能依次输出：

- `--- 检索 query ---`（在启用改写或多查询时，会列出实际用于检索的语句）
- `--- 引用 ---`（检索到的片段 id、类型、来源等）
- `--- 回答 ---`（模型最终回答）

---

## 7. 示例数据位置

示例文档在 `data/docs/`，例如：

- `sample.md`
- `hr/leave_policy.md`、`hr/recruitment.md`
- `it/security_access.md`
- `finance/expense_reimburse.md`
- `legal/data_compliance.md`

入库后即可按下列用例验证。

---

## 8. 建议测试用例（回归）

以下命令均在**项目根目录**执行，且建议先完成一次入库（见第 5.2 节）。

### 8.1 基线：直接提问

```bash
python -m src.rag.main -q "年假至少多少天？"
python -m src.rag.main -q "报销要在多少天内提交？"
python -m src.rag.main -q "VPN 账号丢了怎么办？"
```

**预期**：回答与文档一致；引用区有合理的 `id` / `source`。

### 8.2 按目录或文件过滤 `--source-contains`

只搜 HR 相关路径：

```bash
python -m src.rag.main -q "试用期" --source-contains "hr/"
```

只搜文件名包含某关键词：

```bash
python -m src.rag.main -q "密码长度" --source-contains "security_access"
```

**预期**：引用来源落在限定范围内。

### 8.3 查询改写 `--rewrite`

```bash
python -m src.rag.main -q "报销拖了很久还能报吗？" --rewrite
```

**预期**：出现 `--- 检索 query ---`；回答更贴近文档中关于期限的表述（以模型为准）。

### 8.4 多查询 `--multi-query`

```bash
python -m src.rag.main -q "年假和事假有什么区别？" --multi-query --multi-query-n 3
```

**预期**：检索 query 区域出现多条不同角度的查询句。

### 8.5 重排序 `--rerank`

```bash
python -m src.rag.main -q "公司网络怎么连？" --top-k 6 --rerank --rerank-keep 3
```

**预期**：引用更聚焦；会多消耗一次 LLM 调用。

### 8.6 多轮对话 `--thread-id`

```bash
python -m src.rag.main --thread-id demo -q "年假至少几天？"
python -m src.rag.main --thread-id demo -q "那申请流程呢？"
```

**预期**：第二轮能理解「那」指代上一轮主题。

### 8.7 组合示例

```bash
python -m src.rag.main --thread-id t1 --source-contains "hr/" -q "紧急事假怎么补单？" --rewrite --rerank
```

---

## 9. 常见问题

**问：提示找不到 Key？**  
答：检查项目根是否有 `.env`，且其中 `OPENAI_API_KEY`（或 Deepseek 对应项）已填写。

**问：代理报错或 405？**  
答：`OPENAI_HTTP_PROXY` 只填本机代理地址；API 转发地址填 `OPENAI_BASE_URL`，二者不要混用。

**问：问答没有引用或说知识库为空？**  
答：先执行入库命令，并确认 `data/kb/` 下已生成索引文件。

**问：如何用国内镜像或第三方兼容接口？**  
答：将兼容服务的根 URL 写入 `OPENAI_BASE_URL`，Key 写入 `OPENAI_API_KEY`。

**问：接口路径、请求 JSON 字段记不清？**  
答：以后端 **`http://<主机>:8000/docs`** 为准；也可导出 **`/openapi.json`** 到 Postman/Apifox。

---

## 10. 获取帮助

- 架构与二次开发：见 **[开发文档.md](./开发文档.md)**  
- 仓库入口与 API 速查：见 **[README.md](./README.md)**  
- 前端安装与构建：见 **[frontend/README.md](./frontend/README.md)**
